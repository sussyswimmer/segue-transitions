// Build-time configuration. Nothing here is user-editable from the panel.
//
// BUILT_IN_KEY is the DeepSeek key the extension ships with, so people who install it
// get suggestions without pasting a key of their own. Paste the key between the quotes
// before zipping for the Web Store. Leave it empty in the repository: anyone who can
// read this file — including anyone who unpacks the installed extension — can use the
// key, so treat it as public and keep a spending cap on the DeepSeek account behind it.
//
// A key entered in the panel's Settings still takes precedence over this one.

export const BUILT_IN_KEY = '';
